/**
 * @class User
 * @description Represents a user in the system
 */
export class StudentPostulation {

    constructor({ id, projectId, postulationStatus  }) {
        this.id = id;
        this.projectId = projectId;
        this.postulationStatus = postulationStatus;
    }
}
